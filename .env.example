# Copy to .env for tools that read it, or use secrets.json + --dart-define-from-file
# (see README "Secrets & local setup"). Flutter entrypoint uses --dart-define, not this file by default.
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
