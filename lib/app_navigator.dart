import 'package:flutter/material.dart';

/// Root [NavigatorState] for clearing the stack after sign-out (see [ProfilePage]).
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
